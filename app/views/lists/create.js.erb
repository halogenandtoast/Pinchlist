$('table tr').append("<%= escape_javascript(render(current_user.proxy_for(@list))) %>");
$('.picker').colorPicker();
$('#new_task_title_<%= @list.id %>').focus();
$('#list_title').val("");
enable_task_sorting();
setup_color_pickers();
